<Project Sdk="Microsoft.NET.Sdk">

  <!-- Does not compile on purpose (and is not part of the solution): used to test how build failures are reported. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
  </PropertyGroup>

</Project>
